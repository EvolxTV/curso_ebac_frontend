function multiplicacao(a: number, b: number): number {
  return a * b;
}

const multResult = multiplicacao(20, 10);

console.log(multResult);

function dizOla(nome: string): string {
  return `Olá ${nome}`;
}

const oiSezimar = dizOla("Sezimar");

console.log(oiSezimar);
